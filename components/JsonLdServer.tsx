/** Server-rendered JSON-LD for crawlers (initial HTML). */
export default function JsonLdServer({ data }: { data: Record<string, unknown> }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  );
}
